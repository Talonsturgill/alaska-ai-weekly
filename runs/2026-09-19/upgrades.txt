run_guard.py no longer wedges every future run. A lock whose deliverables are not on disk is a fossil of a previous container and is archived with a note, while a lock with real bytes still demands the full delivery proof. This run couldn't start until it was fixed.
The worklog moved out of .claude/ and into out/, because a harness permission prompt stopped this run mid-Phase-0 and a stop is a failed run. Written into CLAUDE.md and the routine prompt so the next run can't meet it.
New gate say_it_show_it_check.py, now a blocking row in preflight. It compares what each shot DRAWS against what its own VO lines SAY. The routine has carried that lesson in prose since 2026-08-06 and it cost a full render then and five shots today. Verified green on this board and red on a deliberately re-anchored one.
New craft advance lib/forecast.tsx, the forecast grammar. A claim about a TIME, with NOW always drawn, uncertainty that always widens, and a Reconcile whose MEASURED slot starts EMPTY. No future dispatch can draw a confident forecast without the component asking whether anyone checked it.
New library asset lib/runofriver.tsx, the intake, penstock and powerhouse the shelf did not have, with params chosen to say what a run-of-river plant can't do.
New build-time invariant lib/cropsafe.ts, wired into the episode's Plate and Head. The LinkedIn deliverable is the middle square of a 1080x1920 canvas, and five defects in this film came from authoring in canvas coordinates. A nameplate cut in half at the frame edge, the film's central number reduced to a sliver, and a four-line list the square cut never showed at all. The render now throws before a frame is encoded. crop_safety.py had already written down why it isn't the authority itself, because at a boundary row a plate's border and the room's wall-tile seam are the same measurement. This is the check that knows.
Fixed the caption chunker's real gap. build_scenes already guarded against a spoken number splitting across cards, and its number-word list jumps twelve to twenty, so every TEEN fell through and the word point was never in it. The screen read sixty six customers for a co-op with 1,566, and eight megawatts of diesel under a plate saying 10.8 MW. A card may also no longer open on punctuation, after a judge found one that was literally a comma and then nine months.
Found and fixed the reason the film looked static. From shot 4 onward, nine of twelve shots animated beats that fire BEFORE the shot is on screen, by up to nine seconds, so the film built its cards during the previous shot and cut to a settled tableau. Every beat is now timed into the shot that draws it, anchored to a VO line start inside it, with 3.4 seconds of runway before the cut.
New gate strip_name_check.py. Three judges across two panel rounds reported that the strip named for an overshoot contains no bar and the strip named for a character stepping contains no character. They were right about the evidence and the film was not what was wrong. The board files a beat under one shot and the engine draws it in another from beat 10 onward, so 31 of 40 filmstrips were named for a move the film does not make at that moment, and the Motion axis was marked down for it twice. Registered advisory with its own reason written in.
build_evidence now reads either board schema, after it died on this run's board inside a dict.get default that Python evaluates eagerly, and it picks every filmstrip anchor by MEASURED peak motion inside its own beat rather than guessing from the kind of move.
forecast.tsx takes a labelFill. Every label in that grammar was drawn in INK, right on the paper backgrounds it was written for and nearly invisible on a dark wall. The default stays INK so nothing already shipped moves.
Rebuilt the Alaska map from 61 real coastline vertices projected through cos(latitude), after all three judges called the freehand outline a blob with no panhandle and no chain.
Repaired 22 stale beat vo fields that had drifted from the script after the density rewrite, which is exactly the drift the say-it-show-it gate was built to find.
Registered both new art modules in ASSET_MANIFEST.md, including a second written statement of the 2026-08-05 lesson that an absence silhouette must carry the whole subject, because this run's first twin was a dome and read as an egg.
