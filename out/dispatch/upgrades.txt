GEMINI BILLING IS DEPLETED AND IT BLOCKS THE DEFAULT VOICE PATH ON EVERY RUN UNTIL YOU TOP IT UP. The API returns HTTP 402 on every call, not just TTS: "Your prepayment credits are depleted." Fix it at https://ai.studio/projects . Today's film fell back to edge-tts, so the narrator is Andrew Multilingual, not Sulafat.
scripts/dispatch_vo_edge.py: the sanctioned fallback could not run in this container at all. aiohttp does not read HTTPS_PROXY without trust_env, and outbound TLS is re-terminated by the agent proxy so the chain must verify against the proxy CA. Either fault alone produced the same misleading "self-signed certificate in certificate chain".
scripts/dispatch_vo_edge.py: it called the playwright-bundled ffmpeg, which has no mp3 decoder, so it died on edge-tts output with exit 183 and "Invalid data found", which reads like a corrupt download and is not one. It now prefers a system ffmpeg that can decode.
scripts/dispatch_vo_edge.py: it only accepted the original list-of-bare-strings script schema, which the pipeline stopped producing months ago. A fallback that only accepts a shape nothing writes any more is not a fallback.
scripts/captions_from_words.py: NEW. The fallback path had no way to turn forced-alignment words into the cue file build_scenes.py reads, and feeding it the raw alignment failed three layers deep with a bare KeyError.
scripts/captions_from_words.py: it first built cue text from the ASR transcript, and the rough cut burned "Isle -San" for Eielson and "home" for hum. Caption text now comes from the SCRIPT and only the timing comes from the audio, which is DISPATCH_STANDARD section 5. It also honours the script's display spelling, so 4,700 renders as a numeral rather than as "four thousand seven hundred".
scripts/shot_conform_check.py: NEW, and it closes the deadlock the 2026-09-19 run handed forward. strip_name_check and say_it_show_it read the same beat["shot"] field from opposite ends, and that run refiled 22 beats to quiet one, turned the other red, reverted, and left it. They never actually contradicted each other: the board's shot field decides which words play over a beat, so the ENGINE was the wrong side. Measured on that run's own shipped artifacts: 40 beats, 31 disagreements, ALL of them in the engine column and zero board-vs-script, which is the proof.
video-engine/src/lib/tariff.tsx: NEW library asset. The shelf could not draw a BILL, or a cost that splits into named parts and divides across people. Alaska's news is rate cases, tariffs, PCE and borough budgets nearly every week and every one of them is that picture.
video-engine/src/lib/absence.tsx: CRAFT ADVANCE. The grammar drew a missing OBJECT. It now draws a missing VALUE, which is a different problem, because a dashed outline around empty space reads as a box and a box is a thing.
config/brand.yaml line 25 told writers a colon is an acceptable pause while guardrail 5 and caption_check.py both ban it outright. A writer following the brand file would be sent back by the linter.
